import './product-catalog.sass';


// const transformPrice = (value) => {
//   const formatter = new Intl.NumberFormat("ru", {
//     style: "currency",
//     currency: "RUB"
//   });
//   return formatter.format(value)
// }

// const price = document.querySelectorAll(`.product-basket__total-price`);
// const currentPrice = document.querySelectorAll(`.product-basket__price`);

// price.forEach((item) => {
//   item.innerHTML = transformPrice(item.innerHTML);
// });

// currentPrice.forEach((item) => {
//   item.innerHTML = transformPrice(item.innerHTML);
// });